.DS_Store

# Python
__pycache__/
*.pyc

# Generated, regenerable via `python3 build/build.py` (kept out of git for size):
build/orders.json
contracts/test/fixtures/

# Seasonal working dirs (regenerable); raw IPFS originals live in images/seasons + metadata/seasons
build/seasons/data/
build/seasons/out/
build/seasons/out_b/
build/seasons/manifests/

# Kept in git: build/out/ (the ~72KB on-chain deploy blobs + manifest)

# Rendered Hermes pet packs (~470MB, regenerable via build/pets/batch.py).
# Generator code is kept in git; packs are published as a release artifact / IPFS / R2.
build/pets/out/
build/pets/dist/
build/pets/deploy/
build/pets/.wrangler/
build/pets/node_modules/
